//! The `model` module specifies and implements the domain layer types.
//!
//! It encompasses the "core types", so to speak.

// Represents a GTD action.
pub mod action;
// Module containing common entities.
mod common;
